# Backend Runtime Work Execute Prompt

home-search-harness mode=execute

Work item: {{WORK_ID}}
Preset: {{PRESET}}
Target: {{TARGET}}
Branch: {{BRANCH_NAME}}

Allowed edit scope:
- {{ALLOWED_SCOPE}}

Forbidden edit scope:
- {{FORBIDDEN_SCOPE}}
- docs/**
- AGENTS.md
- README.md
- package-lock.json
- build output

Instructions:
- Edit only paths inside the allowed edit scope. If the work item needs a path outside that scope, stop and report the required split.
- Read root AGENTS.md, apps/api/AGENTS.md, CONTEXT.md, apps/api/CONTEXT.md, and relevant canonical docs before editing.
- Preserve the public API contract and data invariants.
- Do not introduce later-scope ranking, favorite, alarm, mail, recommendation, auth, or heavy analytics work.
- Do not store secrets, service keys, .env* files, production data, or build output.
- Treat local Docker/PostGIS work as repository-managed developer runtime only; do not run destructive DB operations.
- Treat `cd apps/api && ./gradlew backendQualityCheck` as the backend canonical PR/CI gate when apps/api changes are present.
- Use the minimum GREEN work item and leave a short Korean-first gate summary.

Skill routing:
{{SKILL_ROUTING}}

Required verification:
- {{VERIFICATION_COMMANDS}}
- Use exact evidence line format: ``- `command` = pass|fail|not run (Korean reason)``.

Final user-facing evidence labels:
- 상태:
- 최초 RED:
- 예상 RED 실패:
- 최소 GREEN:
- 검증:
- 보안 영향:
- security-audit: 지적사항 = none|listed
- 주요 위험:
- 다음 행동:
