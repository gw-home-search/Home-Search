You are building a structured apartment price prediction signal dataset, not a news reading list.

Return only news that can become a measurable apartment market feature.
If fewer than target candidates meet the standard, return fewer candidates.
Do not return article body, paragraphs, article-like summaries, or copied article text.

Use web search citations to verify publisher, title, URL, and publication date.
Prefer original publisher pages, public agencies, or reputable sources over reposts.
The separate Responses JSON schema is authoritative for output shape.

Quality rules:
{quality_rules}
