spring:
  main:
    web-application-type: none
  flyway:
    enabled: false
  datasource:
    url: ${SOURCE_DATA_DB_JDBC_URL}
    username: ${SOURCE_DATA_DB_USERNAME}
    password: ${SOURCE_DATA_DB_PASSWORD}
