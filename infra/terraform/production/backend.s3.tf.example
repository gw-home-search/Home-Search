terraform {
  backend "s3" {
    bucket       = "replace-with-production-state-bucket"
    key          = "home-search/production/terraform.tfstate"
    region       = "ap-northeast-2"
    encrypt      = true
    kms_key_id   = "replace-with-production-state-kms-arn"
    use_lockfile = true
  }
}
