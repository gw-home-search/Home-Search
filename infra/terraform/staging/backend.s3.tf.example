terraform {
  backend "s3" {
    bucket       = "REPLACE_WITH_BOOTSTRAP_OUTPUT"
    key          = "home-search/staging/terraform.tfstate"
    region       = "ap-northeast-2"
    encrypt      = true
    kms_key_id   = "REPLACE_WITH_BOOTSTRAP_OUTPUT"
    use_lockfile = true
  }
}
